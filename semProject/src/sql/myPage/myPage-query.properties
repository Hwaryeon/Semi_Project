showEnrollProject=SELECT RNUM, p_name, status FROM(SELECT ROWNUM RNUM, p_name, status FROM(select distinct p.p_name, pr.status, ir.pay_date, pr.end_date from product p JOIN product_record pr ON ( p.p_id = pr.p_id ) JOIN investment i ON ( i.p_id = p.p_id ) join invest_record ir on (i.invest_id = ir.invest_id) WHERE i.user_id = ? ORDER BY pr.end_date DESC)) WHERE RNUM BETWEEN ? AND ?
showCreateProject=select rnum, p_name, status from(select rownum rnum, p_name, status from(SELECT distinct p.p_name, pr.status, pr.start_date FROM product p JOIN product_record pr ON ( p.p_id = pr.p_id ) WHERE p.user_id = ? order by pr.start_date desc)) where rnum between ? and ?  
insertUserRankingAttachment=insert into attachment (FID, ORIGIN_NAME, CHANGE_NAME, FILE_PATH, UPLOAD_DATE, FILE_LEVEL, DOWNLOAD_COUNT, P_TYPE, STATUS, USER_ID) values (seq_fid.nextval, ?, ?, ?, SYSDATE, ?,DEFAULT,?, DEFAULT, ?)
listCount=SELECT count(distinct p.p_name) FROM product p JOIN product_record pr ON ( p.p_id = pr.p_id ) JOIN investment i ON ( i.p_id = p.p_id )join invest_record ir on (i.invest_id = ir.invest_id) WHERE i.user_id = ?
listCount2=SELECT count(distinct p.p_name) FROM product p JOIN product_record pr ON ( p.p_id = pr.p_id ) WHERE p.user_id = ?
listCountPayment=select count(p.p_name) from product p join investment i on(i.p_id = p.p_id) join invest_record ir on(i.invest_id = ir.invest_id) where i.user_id = ? order by ir.pay_date DESC 
listCountPayment2=select count(p.p_name) from product p join investment i on(i.p_id = p.p_id) join invest_record ir on (ir.invest_id = i.invest_id) where p.user_id = ? and ir.pay_class = 'paid' order by ir.pay_date DESC
businessPaymentBackground=select rnum, pay_date, p_name, price from (select rownum rnum, pay_date, p_name, price from (select ir.pay_date,  p.p_name, ir.price from product p join investment i on(i.p_id = p.p_id) join invest_record ir on (ir.invest_id = i.invest_id) join product_record pr on (p.p_id=pr.p_id) where p.user_id = ? and ir.pay_class = 'paid' order by ir.pay_date DESC)) where rnum between ? and ?
investPaymentBackground=select rnum, pay_date, p_name, price, pay_class from ( select rownum rnum, pay_date, p_name, price, pay_class from ( select ir.pay_date,  p.p_name, ir.price, i.user_id, ir.pay_class from product p join investment i on(i.p_id = p.p_id) join invest_record ir on (ir.invest_id = i.invest_id) where i.user_id = ? order by ir.pay_date DESC)) where rnum between ? and ?
updateGradingStatus=update member set grading_status='evaluate' where user_id = ? 